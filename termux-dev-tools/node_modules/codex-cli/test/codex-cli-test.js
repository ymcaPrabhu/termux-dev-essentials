var mocha = require('mocha'),
    expect = require('expect.js'),
    cdx = require('../lib/codex-cli');

describe('lib/codex-cli.js', function() {
    describe('The codex-cli module', function() {
        it('is an object', function(done) {
            expect(cdx).to.be.an('object');
            done();
        });
    });
});