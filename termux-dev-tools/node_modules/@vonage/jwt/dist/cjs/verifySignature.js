"use strict";
var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
  // If the importer is in node compatibility mode or this is not an ESM
  // file that has been converted to a CommonJS file using a Babel-
  // compatible transform (i.e. "__esModule" has not been set), then set
  // "default" to the CommonJS "module.exports" for node compatibility.
  isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
  mod
));
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);

// lib/verifySignature.ts
var verifySignature_exports = {};
__export(verifySignature_exports, {
  verifySignature: () => verifySignature
});
module.exports = __toCommonJS(verifySignature_exports);
var import_jsonwebtoken = __toESM(require("jsonwebtoken"));
var import_debug = __toESM(require("debug"));

// lib/errors/invalidPrivateKeyError.ts
var InvalidPrivateKeyError = class extends Error {
  constructor() {
    super("Private key must be a string or buffer");
  }
};

// lib/errors/missingPrivateKeyError.ts
var MissingPrivateKeyError = class extends Error {
  constructor() {
    super("Missing private key");
  }
};

// lib/verifySignature.ts
var { verify } = import_jsonwebtoken.default;
var log = (0, import_debug.default)("vonage:jwt:verifySignature");
var verifySignature = (jwt, privateKey) => {
  if (!privateKey) {
    throw new MissingPrivateKeyError();
  }
  if (typeof privateKey !== "string" && !(privateKey instanceof Buffer)) {
    throw new InvalidPrivateKeyError();
  }
  try {
    verify(jwt, privateKey, {
      algorithms: ["RS256", "HS256"]
    });
    return true;
  } catch (error) {
    log("Error when verifying token", error);
  }
  return false;
};
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  verifySignature
});
