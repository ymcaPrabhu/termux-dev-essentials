/**
 * Enum representing media modes for a session.
 */
declare enum MediaMode {
    /**
     * Routed mode where media is disabled.
     */
    ROUTED = "disabled",
    /**
     * Relayed mode where media is enabled.
     */
    RELAYED = "enabled"
}

export { MediaMode };
