import { SMSMessages } from '../../types/SMSMessages.js';
import { SMSFailure } from './SMSFailure.js';
import '../../types/ErrorMessage.js';
import '../../types/Message.js';
import '../../types/Responses/SMSMessageResponse.js';
import '../../enums/SMSStatus.js';
import '../../types/Responses/SMSResponse.js';
import '../../types/Responses/SMSErrorMessageResponse.js';

/**
 * Class representing a failure when sending all SMS messages.
 *
 * Extends the SMSFailure class and is used to indicate that all SMS messages failed to send.
 */
declare class MessageSendAllFailure extends SMSFailure {
    /**
     * Creates an instance of MessageSendAllFailure.
     *
     * @param {SMSMessages} response - The response containing details about the failed SMS messages.
     */
    constructor(response: SMSMessages);
}

export { MessageSendAllFailure };
