import { SMSMessages } from '../../types/SMSMessages.js';
import { SMSFailure } from './SMSFailure.js';
import '../../types/ErrorMessage.js';
import '../../types/Message.js';
import '../../types/Responses/SMSMessageResponse.js';
import '../../enums/SMSStatus.js';
import '../../types/Responses/SMSResponse.js';
import '../../types/Responses/SMSErrorMessageResponse.js';

/**
 * Class representing a partial failure when sending SMS messages.
 *
 * Extends the SMSFailure class and is used to indicate that some SMS messages failed to send.
 */
declare class MessageSendPartialFailure extends SMSFailure {
    /**
     * Creates an instance of MessageSendPartialFailure.
     *
     * @param {SMSMessages} response - The response containing details about the partially failed SMS messages.
     */
    constructor(response: SMSMessages);
}

export { MessageSendPartialFailure };
