import { AuthParams, AuthInterface } from '@vonage/auth';
import { VetchOptions } from '@vonage/vetch';

/**
 * Represents the parameters for configuring the NumbersClass.
 *
 * @deprecated This is no longer in use
 */
type NumbersClassParameters = AuthParams & VetchOptions & {
    /**
     * The authentication configuration.
     */
    auth?: AuthInterface;
};

export type { NumbersClassParameters };
