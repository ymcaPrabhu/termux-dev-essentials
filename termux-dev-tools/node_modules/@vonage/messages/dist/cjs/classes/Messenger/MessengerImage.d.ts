import { Channels } from '../../enums/Channels.js';
import { MessengerType } from '../../types/Channels/Messenger/MessengerType.js';
import { MessengerImageParams } from '../../types/Channels/Messenger/MessengerImageParams.js';
import { AbstractImageMessage } from '../AbstractImageMessage.js';
import '../../enums/Messenger/MessengerCategory.js';
import '../../types/MessageParamsImage.js';
import '../../types/MessageParams.js';
import '../../enums/MessageTypes.js';
import '../../types/MessageImageType.js';
import '../../types/Channels/Messenger/MessengerParams.js';
import '../AbstractMessage.js';

/**
 * Represents an image message for the Messenger channel.
 *
 * This class extends the `AbstractImageMessage` class and implements the `MessengerImageParams` interface.
 * It is used for sending image messages on the Messenger channel.
 *
 * @group Messenger
 */
declare class MessengerImage extends AbstractImageMessage implements MessengerImageParams {
    /**
     * The channel for this message (always 'messenger').
     */
    channel: Channels.MESSENGER;
    /**
     * Additional Messenger-specific parameters for the image message.
     */
    messenger: MessengerType;
    /**
     * Send an image message using the Facebook Messenger channel.
     *
     * @param {MessengerImageParams} params - The parameters for the image message.
     * @example
     * ```ts
     * import { MessengerImage } from '@vonage/messages';
     *
     * const { messageUUID } = await messagesClient.send(new MessengerImage({
     *  to: TO_NUMBER,
     *  from: FROM_NUMBER,
     *  image: {
     *    url: 'https://example.com/image.jpg',
     *    caption: 'This is an image',
     *  },
     *  clientRef: 'my-personal-reference',
     * }));
     *
     * console.log(`Message sent successfully with UUID ${messageUUID}`);
     * ```
     */
    constructor(params: Omit<MessengerImageParams, 'channel' | 'messageType'>);
}

export { MessengerImage };
