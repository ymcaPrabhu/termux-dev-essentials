import { AbstractVideoMessage } from '../AbstractVideoMessage.js';
import { Channels } from '../../enums/Channels.js';
import { WhatsAppContext } from '../../types/Channels/WhatsApp/WhatsAppParams.js';
import { WhatsAppVideoParams } from '../../types/Channels/WhatsApp/WhatsAppVideoParams.js';
import '../AbstractMessage.js';
import '../../enums/MessageTypes.js';
import '../../types/MessageParams.js';
import '../../types/MessageParamsVideo.js';
import '../../types/MessageVideoType.js';

/**
 * Represents a video message for WhatsApp.
 *
 * @group WhatsApp
 */
declare class WhatsAppVideo extends AbstractVideoMessage implements WhatsAppVideoParams {
    /**
     * The channel for this message (always 'whatsapp').
     */
    channel: Channels.WHATSAPP;
    context?: WhatsAppContext;
    /**
     * Sends a video message to a WhatsApp user.
     *
     * @param {WhatsAppVideoParams} params - The parameters for creating a WhatsApp video message.
     *
     * @example
     * ```ts
     * import { WhatsAppVideo } from '@vonage/messages';
     *
     * const { messageUUID } = await messagesClient.send(new WhatsAppVideo({
     *  to: TO_NUMBER,
     *  from: FROM_NUMBER,
     *  video: {
     *    url: 'https://example.com/video.mp4',
     *    caption: 'This is a video message',
     *  },
     *  clientRef: 'my-personal-reference',
     * }));
     *
     * console.log(`Message sent successfully with UUID ${messageUUID}`);
     * ```
     */
    constructor(params: Omit<WhatsAppVideoParams, 'channel' | 'messageType'>);
}

export { WhatsAppVideo };
