import { AbstractMessage } from './AbstractMessage.mjs';
import { MessageTypes } from '../enums/MessageTypes.mjs';
import { MessageParamsText } from '../types/MessageParamsText.mjs';
import '../enums/Channels.mjs';
import '../types/MessageParams.mjs';

/**
 * An abstract base class for text message objects.
 */
declare abstract class AbstractTextMessage extends AbstractMessage implements MessageParamsText {
    /**
     * The type of message (always 'text').
     */
    messageType: MessageTypes.TEXT;
    /**
     * The text content of the message.
     */
    text: string;
    /**
     * Constructs a new `AbstractTextMessage` instance for text messages.
     *
     * @param {MessageParamsText} params - The parameters for creating a text message.
     */
    constructor(params: Omit<MessageParamsText, 'channel' | 'messageType'>);
}

export { AbstractTextMessage };
