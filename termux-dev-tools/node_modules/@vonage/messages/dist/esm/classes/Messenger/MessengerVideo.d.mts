import { AbstractVideoMessage } from '../AbstractVideoMessage.mjs';
import { Channels } from '../../enums/Channels.mjs';
import { MessengerType } from '../../types/Channels/Messenger/MessengerType.mjs';
import { MessengerVideoParams } from '../../types/Channels/Messenger/MessengerVideoParams.mjs';
import '../AbstractMessage.mjs';
import '../../enums/MessageTypes.mjs';
import '../../types/MessageParams.mjs';
import '../../types/MessageParamsVideo.mjs';
import '../../types/MessageVideoType.mjs';
import '../../enums/Messenger/MessengerCategory.mjs';
import '../../types/Channels/Messenger/MessengerParams.mjs';

/**
 * Represents a video message for the Messenger channel.
 *
 * @group Messenger
 */
declare class MessengerVideo extends AbstractVideoMessage implements MessengerVideoParams {
    /**
     * The channel for this message (always 'messenger').
     */
    channel: Channels.MESSENGER;
    /**
     * The messenger information for this message.
     */
    messenger: MessengerType;
    /**
     * Send a video message using the Facebook Messenger channel.
     *
     * @param {MessengerVideoParams} params - The parameters for creating a Messenger video message.
     * @example
     * ```ts
     * import { MessagengerVideo } from '@vonage/messages';
     *
     * const { messageUUID } = await messagesClient.send(new MessagengerVideo({
     *  to: TO_NUMBER,
     *  from: FROM_NUMBER,
     *  video: {
     *    url: 'https://example.com/video.mp4',
     *    caption: 'This is a video',
     *  },
     *  clientRef: 'my-personal-reference',
     * }));
     *
     * console.log(`Message sent successfully with UUID ${messageUUID}`);
     * ```
     */
    constructor(params: Omit<MessengerVideoParams, 'channel' | 'messageType'>);
}

export { MessengerVideo };
