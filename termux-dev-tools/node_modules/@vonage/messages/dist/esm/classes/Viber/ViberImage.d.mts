import { AbstractImageMessage } from '../AbstractImageMessage.mjs';
import { Channels } from '../../enums/Channels.mjs';
import { ViberActionParams } from '../../types/Channels/Viber/ViberActionParams.mjs';
import { ViberImageParams } from '../../types/Channels/Viber/ViberImageParams.mjs';
import '../AbstractMessage.mjs';
import '../../enums/MessageTypes.mjs';
import '../../types/MessageParams.mjs';
import '../../types/MessageParamsImage.mjs';
import '../../types/MessageImageType.mjs';
import '../../types/Channels/Viber/ViberService.mjs';
import '../../enums/Viber/ViberCategory.mjs';
import '../../types/Channels/Viber/ViberAction.mjs';
import '../../types/Channels/Viber/ViberParams.mjs';

/**
 * Represents an image message for the Viber Service channel.
 *
 * @group Viber
 */
declare class ViberImage extends AbstractImageMessage implements ViberImageParams {
    /**
     * The channel for this message (always 'viber_service').
     */
    channel: Channels.VIBER;
    viberService: ViberActionParams;
    /**
     * Send an image message using the Viber Service channel.
     *
     * @param {ViberImageParams} params - The parameters for the ViberImage message.
     * @example
     * ```ts
     * import { ViberImage } from '@vonage/messages';
     *
     * const { messageUUID } = await messagesClient.send(new ViberImage({
     *  to: TO_NUMBER,
     *  from: FROM_NUMBER,
     *  image: {
     *  url: 'https://my-host.com/my-image.jpg',
     *  },
     *  viberService: {
     *    action: {
     *      url: 'https://my-host.com/my-path',
     *      text: 'My button text',
     *    },
     *  },
     *  clientRef: 'my-personal-reference',
     * }));
     *
     * console.log(`Message sent successfully with UUID ${messageUUID}`);
     * ```
     */
    constructor(params: Omit<ViberImageParams, 'channel' | 'messageType'>);
}

export { ViberImage };
