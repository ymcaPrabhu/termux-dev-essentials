import { AbstractTextMessage } from '../AbstractTextMessage.mjs';
import { Channels } from '../../enums/Channels.mjs';
import { WhatsAppContext } from '../../types/Channels/WhatsApp/WhatsAppParams.mjs';
import { WhatsAppTextParams } from '../../types/Channels/WhatsApp/WhatsAppTextParams.mjs';
import '../AbstractMessage.mjs';
import '../../enums/MessageTypes.mjs';
import '../../types/MessageParams.mjs';
import '../../types/MessageParamsText.mjs';

/**
 * Represents a text message for WhatsApp.
 *
 * @group WhatsApp
 */
declare class WhatsAppText extends AbstractTextMessage implements WhatsAppTextParams {
    /**
     * The channel for this message (always 'whatsapp').
     */
    channel: Channels.WHATSAPP;
    context?: WhatsAppContext;
    /**
     * Send a WhatsApp text message.
     *
     * @param {WhatsAppTextParams} params - The parameters for creating a WhatsApp text message.
     * @example
     * ```ts
     * import { WhatsAppText } from '@vonage/messages';
     *
     * const { messageUUID } = await messagesClient.send(new WhatsAppText({
     *  to: TO_NUMBER,
     *  from: FROM_NUMBER,
     *  text: 'Hello world',
     *  clientRef: 'my-personal-reference',
     * }));
     *
     * console.log(`Message sent successfully with UUID ${messageUUID}`);
     * ```
     */
    constructor(params: Omit<WhatsAppTextParams, 'channel' | 'messageType'>);
}

export { WhatsAppText };
