import { AbstractAudioMessage } from '../AbstractAudioMessage.mjs';
import { Channels } from '../../enums/Channels.mjs';
import { WhatsAppAudioParams } from '../../types/Channels/WhatsApp/WhatsAppAudioParams.mjs';
import { WhatsAppContext } from '../../types/Channels/WhatsApp/WhatsAppParams.mjs';
import '../AbstractMessage.mjs';
import '../../enums/MessageTypes.mjs';
import '../../types/MessageParams.mjs';
import '../../types/MessageParamsAudio.mjs';
import '../../types/MessageAudioType.mjs';

/**
 * Represents an audio message for WhatsApp.
 *
 * @group WhatsApp
 */
declare class WhatsAppAudio extends AbstractAudioMessage implements WhatsAppAudioParams {
    /**
     * The channel for this message (always 'whatsapp').
     */
    channel: Channels.WHATSAPP;
    context?: WhatsAppContext;
    /**
     * Sends an audio message to a WhatsApp user.
     *
     * @param {WhatsAppAudioParams} params - The parameters for creating a WhatsApp audio message.
     * @example
     * ```ts
     * import { WhatsAppAudio } from '@vonage/messages';
     *
     * const { messageUUID } = await messagesClient.send(new WhatsAppAudio({
     *  to: TO_NUMBER,
     *  from: FROM_NUMBER,
     *  audio: {
     *    url: 'https://example.com/audio.mp3',
     *    caption: 'This is an audio message',
     *  },
     *  clientRef: 'my-personal-reference',
     * }));
     *
     * console.log(`Message sent successfully with UUID ${messageUUID}`);
     * ```
     */
    constructor(params: Omit<WhatsAppAudioParams, 'channel' | 'messageType'>);
}

export { WhatsAppAudio };
