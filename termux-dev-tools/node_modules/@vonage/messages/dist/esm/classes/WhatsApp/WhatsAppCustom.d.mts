import { AbstractMessage } from '../AbstractMessage.mjs';
import { Channels } from '../../enums/Channels.mjs';
import { MessageTypes } from '../../enums/MessageTypes.mjs';
import { WhatsAppContext } from '../../types/Channels/WhatsApp/WhatsAppParams.mjs';
import { WhatsAppCustomParams } from '../../types/Channels/WhatsApp/WhatsAppCustomParams.mjs';
import { WhatsAppCustomType } from '../../types/Channels/WhatsApp/WhatsAppCustomType.mjs';
import '../../types/MessageParams.mjs';

/**
 * Represents a custom message for WhatsApp.
 *
 * @group WhatsApp
 */
declare class WhatsAppCustom extends AbstractMessage implements WhatsAppCustomParams {
    /**
     * The channel for this message (always 'whatsapp').
     */
    channel: Channels.WHATSAPP;
    /**
     * The type of message (always 'custom').
     */
    messageType: MessageTypes;
    custom: WhatsAppCustomType;
    context?: WhatsAppContext;
    /**
     * Sends a custom message to a WhatsApp user.
     *
     * @param {WhatsAppCustomParams} params - The parameters for creating a WhatsApp custom message.
     * @example
     * ```ts
     * import { WhatsAppCustom } from '@vonage/messages';
     *
     * const { messageUUID } = await messagesClient.send(new WhatsAppCustom({
     *  to: TO_NUMBER,
     *  from: FROM_NUMBER,
     *  custom: {
     *    type: 'template',
     *    template: {
     *      namespace: 'your-namespace',
     *      name: 'your-template-name',
     *    },
     *  }
     *  clientRef: 'my-personal-reference',
     * }));
     *
     * console.log(`Message sent successfully with UUID ${messageUUID}`);
     * ```
     */
    constructor(params: Omit<WhatsAppCustomParams, 'channel' | 'messageType'>);
}

export { WhatsAppCustom };
