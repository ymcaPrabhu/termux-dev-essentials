import { WhatsAppChannelInterface } from './WhatsAppChannelInterface.mjs';
import { MessageInterface } from '../MessageInterface.mjs';
import { WhatsAppStickerIdType } from '../../types/Channels/WhatsApp/WhatsAppStickerIdType.mjs';
import { WhatsAppStickerUrlType } from '../../types/Channels/WhatsApp/WhatsAppStickerUrlType.mjs';

/**
 * Represents an interface for WhatsApp sticker messages.
 *
 * @deprecated Please use types instead of interfaces.
 *
 * @ignore
 */
interface WhatsAppStickerInterface extends WhatsAppChannelInterface, MessageInterface {
    /**
     * Specifies the type of message, which is "sticker" for sticker messages.
     */
    messageType: 'sticker';
    /**
     * The sticker to be sent. It can be either a sticker URL or a sticker ID.
     */
    sticker: WhatsAppStickerUrlType | WhatsAppStickerIdType;
}

export type { WhatsAppStickerInterface };
